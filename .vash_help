Usage: ./minishell [flags] [command]

flags long:
	-- single	Takes as third argument a string including the command that needs to be executed. 
			Opens minishell for this single command and exits afterwards

flags short:
	-s		[same as --single]
