Usage: ./minishell [flags] [command]

flags:
	-- single / -s	Takes as third argument a string including the command that needs to be executed. 
			Opens minishell for this single command and exits afterwards
